
/* config for the first time /*
git config --global user.email "ductrienx1@gmail.com"
git config --global user.name "ductrien"

/* add a new file or modify it
git status
git add *
git commit -m"Add new file Trien.txt"
git push


/*back up
git log
copy the code 
git revert [code]
git commit -m"comment"
git push

/*edit on github
edit and commit changes
git pull

/*remove commit( sau khi add ma khong muon commit len local)
git rm --cached <filename>

/*commit
git commit -a -m"comment" (commit tất cả change luôn, k cần add)
get commit --amend -a -m"comment" (gộp với commit trước thành 1, khi change quá ít
trình edit ra, bấm a để edit commit trước, rồi bấm :wq để write and quit)
